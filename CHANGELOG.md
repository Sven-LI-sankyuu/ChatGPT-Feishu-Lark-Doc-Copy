# 更新记录

## 0.1.3

### 修复

- 恢复官方复制在公式块中误转为 `##` 二级标题的减号，避免公式内容混入标题标记。
- 清理官方复制额外插入的公式内部空行，正文标题保持原样。

## 0.1.2

### 修复

- 飞书按钮改为读取相邻官方复制按钮生成的 Markdown，再转换飞书公式定界符，避免从 KaTeX 视觉 DOM 复制出损坏公式。
- 支持官方复制结果中的 `\[...\]`、独占行 `[...]`、`\(...\)` 和可明确识别的行内 LaTeX 变量。
- 官方复制结果无法确认时停止写入剪贴板并显示失败状态，禁止把视觉字符伪装成公式。
- 按钮增加不可见版本标记，浏览器测试会确认实际加载的是当前构建版本。

## 0.1.1

### 改进

- 提高复制按钮的默认对比度，使其接近 ChatGPT 官方操作栏图标。
- 默认移除块之间的非必要空行，同时保留代码块和公式内部换行。
- 在开发原则中加入设置页面、默认配置和可扩展配置结构设计。

## 0.1.0

### 新增

- 在 ChatGPT 官方回复复制按钮旁增加独立的“复制飞书文档版”按钮。
- 完整复制按钮所属的助手回复，不受当前屏幕可视范围限制。
- 将标题、段落、强调、列表、引用、代码、公式、表格和链接地址转换为飞书可解析的 Markdown。
- 支持 KaTeX 与 MathJax 原始 LaTeX 提取，并保留行内与独立公式边界。
- 提供浅色与深色主题适配、悬停提示、成功与错误状态。
- 提示气泡显示在按钮下方，并挂在页面顶层以避免被 ChatGPT 操作栏裁切。
- 提供自动测试、真实 Chromium 扩展测试、版本化 ZIP 构建和飞书人工验收材料。

### 兼容性

- 真实 ChatGPT 到飞书测试与验收样本已通过标题、公式、表格、超链接、基础粗体、基础斜体、下划线和删除线。
- 独立公式可识别但需要在飞书中手动居中。
- 粗斜体组合与行内代码在不同飞书样本中的识别结果不一致，留待后续版本专项处理。
- 相邻的粗体、斜体、粗斜体与删除线 Markdown 输出已加入自动回归测试。
